node_modules/
# campaigns saved by a running server: players' data, not source
campaigns/
# screenshots a few older harnesses drop in the project root
/shot-*.png

# the built single-file pages: `npm run build` makes them
build/
test/art/diff/
